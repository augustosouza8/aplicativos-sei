# Vari�veis de ambiente para o projeto SEI
# Copie este arquivo para .env e preencha com suas credenciais reais
# 
# IMPORTANTE: O arquivo .env n�o deve ser commitado no reposit�rio (j� est� no .gitignore)

# ============================================================================
# CREDENCIAIS OBRIGAT�RIAS
# ============================================================================

# Login do SEI
SEI_USER=seu_login_aqui

# Senha do SEI
SEI_PASS=sua_senha_aqui

# C�digo do �rg�o SEI (ex: 28 para SEPLAG, 52 para FHEMIG)
SEI_ORGAO=28

# Nome da unidade SEI desejada (deve corresponder exatamente ao nome no sistema)
# Para descobrir o nome exato: fa�a login no SEI e clique no link de unidade no topo
# Exemplos: "SEPLAG/AUTOMATIZAMG", "FHEMIG/DIRASS/GEPI/CIP/CFA"
SEI_UNIDADE=SEPLAG/AUTOMATIZAMG

# ============================================================================
# CONFIGURA��ES OPCIONAIS
# ============================================================================

# Ativa logs detalhados (1 para ativar, 0 ou vazio para desativar)
SEI_DEBUG=1

# Salva HTMLs de debug em data/debug/ (1 para ativar)
# �til para auditoria ou troubleshooting; desative em produ��o
SEI_SAVE_DEBUG_HTML=1

# Diret�rio base para dados persistentes (hist�rico, iframes, etc.)
# SEI_DATA_DIR=data

# ============================================================================
# FILTROS OPCIONAIS (podem ser usados via CLI tamb�m)
# ============================================================================

# Filtro de visualiza��o: "visualizados" ou "nao_visualizados"
# SEI_FILTRO_VISUALIZACAO=nao_visualizados

# Categorias: "recebidos", "gerados" ou ambos separados por v�rgula
# SEI_FILTRO_CATEGORIA=recebidos

# Respons�veis (substring, separados por v�rgula)
# SEI_FILTRO_RESPONSAVEL=Jo�o, Maria

# Tipos/Especificidade (substring, separados por v�rgula)
# SEI_FILTRO_TIPO=Est�gio, Viagem

# Marcadores (substring, separados por v�rgula)
# SEI_FILTRO_MARCADOR=assinatura, simplifica��o

# Limite m�ximo de processos ap�s aplicar filtros
# SEI_FILTRO_LIMITE=10

# ============================================================================
# EXPORTA��O E HIST�RICO
# ============================================================================

# Caminho para exportar automaticamente para Excel
# SEI_EXPORTAR_XLSX=saida/processos.xlsx

# Ativa salvamento de hist�rico em JSON (1 para ativar)
# SEI_SALVAR_HISTORICO=1

# Caminho do arquivo de hist�rico (default: data/historico_processos.json)
# SEI_HISTORICO_ARQUIVO=data/historico_processos.json

# ============================================================================
# COLETA DE DOCUMENTOS
# ============================================================================

# Ativa coleta de metadados de documentos do iframe (1 para ativar)
# SEI_COLETAR_DOCUMENTOS=1

# Limite de processos para coleta detalhada de documentos
# SEI_LIMITE_PROCESSOS_DOCUMENTOS=5

# Salva HTMLs dos iframes em disco (1 para ativar)
# SEI_DUMP_IFRAMES=1

# Limite de iframes salvos quando dump est� ativo
# SEI_DUMP_IFRAMES_LIMITE=5

# Diret�rio para salvar iframes (default: data/iframes)
# SEI_DUMP_IFRAMES_DIR=data/iframes

# ============================================================================
# PAGINA��O
# ============================================================================

# M�ximo de p�ginas a carregar de Recebidos
# SEI_PAGINAS_RECEBIDOS=10

# M�ximo de p�ginas a carregar de Gerados
# SEI_PAGINAS_GERADOS=10

# Limite geral de p�ginas (aplica a ambas as categorias)
# SEI_PAGINAS_MAX=5

# ============================================================================
# DOWNLOAD DE PDFs EM LOTE
# ============================================================================

# Ativa download em lote de PDFs (1 para ativar)
# SEI_DOWNLOAD_LOTE=1

# Limite de processos para download em lote
# SEI_MAX_PROCESSOS_PDF=10

# Diret�rio de sa�da dos PDFs gerados (default: diret�rio atual)
# SEI_PDF_DIR=./pdfs

# Habilita processamento paralelo (1 para ativar)
# SEI_PDF_PARALELO=1

# N�mero de workers no modo paralelo (default: 3)
# SEI_PDF_WORKERS=3

# N�mero de tentativas por processo (default: 3)
# SEI_PDF_RETRIES=3



# ============================================================================  
# RELAT�"RIO DI� RIO POR E-MAIL
# ============================================================================  

# M�ximo de processos novos a analisar por dia (default: 10)
# SEI_REL_MAX_PROCESSOS_NOVOS_DIA=10

# Tamanho m�ximo de PDF em MB para considerar v�lido (default: 100)
# PDFs maiores que este limite serão ignorados
# SEI_REL_MAX_TAMANHO_PDF_MB=100

# Caminho do arquivo XLSX do relatório di�rio (default: saida/relatorio_diario.xlsx)
# SEI_REL_XLSX_PATH=saida/relatorio_diario.xlsx

# Diretório para salvar PDFs do relatório di�rio (default: pdfs/relatorio_diario)
# SEI_REL_PDF_DIR=pdfs/relatorio_diario

# E-mail remetente do relatório (OBRIGAT�"RIO para envio)
SEI_REL_EMAIL_FROM=noreply@exemplo.org.br

# Lista de destinat�rios separados por v�rgula (OBRIGAT�"RIO para envio)
SEI_REL_EMAIL_TO=destinatario1@exemplo.org.br,destinatario2@exemplo.org.br

# Servidor SMTP para envio de e-mail (OBRIGAT�"RIO para envio)
# Exemplos: smtp.gmail.com, smtp.office365.com
SEI_REL_SMTP_HOST=smtp.gmail.com

# Porta do servidor SMTP (default: 587)
# Geralmente 587 para TLS ou 465 para SSL
# SEI_REL_SMTP_PORT=587

# Usu�rio para autenticação SMTP (OBRIGAT�"RIO para envio)
SEI_REL_SMTP_USER=noreply@exemplo.org.br

# Senha para autenticação SMTP (OBRIGAT�"RIO para envio)
# Nota: Para Gmail, use "Senha de App", não a senha normal da conta
SEI_REL_SMTP_PASS=sua_senha_smtp

# Habilita TLS para SMTP (default: true)
# Use "true" para porta 587, "false" para porta 465 (SSL)
# SEI_REL_SMTP_USE_TLS=true